<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Timer
    |--------------------------------------------------------------------------
    |
    | Total game time in seconds.
    | This should always equal the sum of duration_by_level.
    |
    */

    'timer' => [
        'initial_seconds' => env('GAME_INITIAL_TIME_SECONDS', 80),
    ],

    /*
    |--------------------------------------------------------------------------
    | Scoring Rules
    |--------------------------------------------------------------------------
    */

    'scoring' => [
        'points_per_pair'        => env('GAME_POINTS_PER_PAIR', 100),
        'time_bonus_per_second'  => env('GAME_TIME_BONUS_PER_SECOND', 5),
        'move_penalty_per_extra' => env('GAME_MOVE_PENALTY_PER_EXTRA', 5),
    ],

    /*
    |--------------------------------------------------------------------------
    | Game Levels
    |--------------------------------------------------------------------------
    |
    | The frontend reads this configuration from:
    | GET /api/v1/game/config
    |
    | The backend also validates progress using these values.
    |
    */

    'levels' => [

        /*
        |--------------------------------------------------------------------------
        | Number of Levels
        |--------------------------------------------------------------------------
        */

        'max_level' => env('GAME_MAX_LEVEL', 2),

        /*
        |--------------------------------------------------------------------------
        | Maximum pairs allowed in a single level (legacy support)
        |--------------------------------------------------------------------------
        */

        'max_pairs_level' => env('GAME_MAX_PAIRS_PER_LEVEL', 10),

        /*
        |--------------------------------------------------------------------------
        | Pairs Per Level
        |--------------------------------------------------------------------------
        */

        'pairs_by_level' => [
            1 => 8,
            2 => 10,
        ],

        /*
        |--------------------------------------------------------------------------
        | Time Per Level (seconds)
        |--------------------------------------------------------------------------
        |
        | Level 1 = 45 sec
        | Level 2 = 35 sec
        | Total = 80 sec
        |
        */

        'duration_by_level' => [
            1 => 45,
            2 => 35,
        ],

        /*
        |--------------------------------------------------------------------------
        | Grid Columns Per Level
        |--------------------------------------------------------------------------
        */

        'cols_by_level' => [
            1 => 4,
            2 => 4,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Game Events to Log
    |--------------------------------------------------------------------------
    */

    'loggable_events' => [
        'game_started',
        'level_completed',
        'game_completed',
    ],

];